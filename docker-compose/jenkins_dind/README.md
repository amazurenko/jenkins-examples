This forlder contais the following:
 - docker-compose.yml
      Compose file will spin up vanila Jenkins with few pre-installed plugins. 
      
      Jenkins contains pipeline job to build hello-world application
